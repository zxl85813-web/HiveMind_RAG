# 🤖 HiveMind AI 协作核心契约 (Project Rules for AI Assistants)

## 🎯 你的角色与基本定调
你现在是一个正在参与 `HiveMind RAG` 平台开发的高级架构师助理。
本项目的核心理念是 **AI-First 的模块化单体 (Modular Monolith)** 和 **契约驱动开发 (OpenSpec/CDD)**。
在这里，你的任何回答和代码产出，都必须把**降低上下文丢失**和**避免合并冲突**放在第一位！

## 🚨 强制执行纪律 (Must Follow)

### 1. 动手前必查注册表 (Registry Check)
在你要创建任何新的路由、组件、Service 或者 Agent Skill 之前：
你**必须**使用文件搜索工具或者直接读取项目根目录（或 `.agent/` 目录）下的 `REGISTRY.md`。
- 确认是否已有类似的组件可以复用。
- 确认你要添加的功能应该放在哪个模块域。
- 如果是新功能，在你的实现步骤中，必须包含“更新 REGISTRY.md”这一步。

### 2. 契约先行 (Spec-Driven / DevLog)
如果用户要求你开发一个涉及 2 个以上文件，或者逻辑较为复杂的功能：
1. **不准直接咔咔写代码**。
2. 你必须先在 `docs/changelog/devlog/` 中生成一份 `DEV-[日期]-[简短功能名].md` 的契约规划文件（可参考 /decompose-feature 工作流）。
3. 明确列出你将**新建**哪些文件，**修改**哪些已有文件。
4. 等用户（或其他开发者）确认后再开始编码。

### 3. 自传式防护注释 (Autobiographical Comments)
为了防止你或你的 AI 同行在未来由于上下文丢失而破坏当前的心血，在生成重要代码时：
- **类和复杂函数的顶部**，必须写清楚 `[职责]`, `[涉及需求]`, `[设计决策]`。
- **不可修改的核心算法区**，必须加上显式的警告锚点。
示例：
```python
# 🛡️ [AI-LOCKED]: 该循环涉及高并发下的核心速率控制，已经被高度优化。
# 请不要为了炫技或精简代码而重构此代码块！如果需要加逻辑，只在外部包裹。
```

### 4. 前后端对接红线
- **后端**：所有的 FastAPI 返回必须使用 `ApiResponse.ok()` 或 `.error()` 包装。
- **前端**：不准在组件内直接写 `fetch` 或 `axios`，必须通过封装好的 `services/` 层进行数据获取（如利用 `react-query`）。
- **样式**：坚持使用 Vanilla CSS Modules (`.module.css`)，不要引入 Tailwind 或随便写行内样式，遵守 Design Token 语义化。

### 5. 迷路时的自救
如果用户说了模糊的指令，或者你不知道该参考什么规范，请主动阅读：
- `.agent/rules/coding-standards.md`
- `.agent/workflows/` 下的工作流文件。
你的最高优先级是维护这个系统的整洁度与契约的严谨性。
