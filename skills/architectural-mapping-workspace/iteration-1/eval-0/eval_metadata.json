{
  "case_id": "lookup-requirement-context",
  "name": "Lookup Requirement Context",
  "assertions": [
    {
      "description": "Uses the graph query script",
      "expected": "query_architecture.py --req"
    },
    {
      "description": "Output is used to limit file reading scope",
      "expected": "collect(DISTINCT f.path)"
    }
  ]
}
