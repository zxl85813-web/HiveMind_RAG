version: '3.9'

services:
  # === Database (PostgreSQL) ===
  postgres:
    image: postgres:16-alpine
    container_name: hivemind-postgres-prod
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-hivemind}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    # No ports exposed to host to avoid conflicts
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data

  # === Cache (Redis) ===
  redis:
    image: redis:7-alpine
    container_name: hivemind-redis-prod
    restart: always
    # No ports exposed to host
    volumes:
      - redis_data_prod:/data

  # === Vector Store (ChromaDB) ===
  chroma:
    image: chromadb/chroma:latest
    container_name: hivemind-chroma-prod
    restart: always
    # No ports exposed to host
    volumes:
      - chroma_data_prod:/chroma/chroma

  # === Object Store (MinIO) ===
  minio:
    image: minio/minio:latest
    container_name: hivemind-minio-prod
    restart: always
    command: server /data --console-address ":9011"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    ports:
      - "9010:9000" # Keep MinIO access if needed for management
      - "9011:9011"
    volumes:
      - minio_data_prod:/data

  # === Backend (FastAPI) ===
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: hivemind-backend-prod
    restart: always
    depends_on:
      - postgres
      - redis
      - chroma
      - minio
    # Remove ports for background access, now proxied by frontend
    env_file:
      - ./backend/.env.production
    environment:
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@postgres:5432/hivemind
      REDIS_URL: redis://redis:6379/0
    # Healthcheck: Docker 每 10s 检查一次，确认 backend 真正就绪后才允许 frontend 启动
    # 使用 python stdlib urllib，避免 runtime 镜像中无 curl 的问题
    healthcheck:
      test: [ "CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8888/health/')" ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  # === Frontend (React / Vite / Nginx) ===
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: hivemind-frontend-prod
    restart: always
    ports:
      - "80:80"
    # 等待 backend 健康检查通过后才启动，确保 Nginx 能解析到 backend 主机名
    depends_on:
      backend:
        condition: service_healthy

volumes:
  postgres_data_prod:
  redis_data_prod:
  chroma_data_prod:
  minio_data_prod:
