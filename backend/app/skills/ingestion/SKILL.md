---
name: ingestion
description: "Handles file ingestion, parsing, and standardized resource extraction."
version: "0.1.0"
---

# Ingestion Skill

This skill provides tools for parsing various document formats (PDF, Excel, Word) into a standardized resource format.
It wraps the `Mineru` parser and other plugins.

## Tools
- `parse_file`: Parse a file and return structured content.
