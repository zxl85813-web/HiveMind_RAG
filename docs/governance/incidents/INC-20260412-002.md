# 🛡️ Governance Incident: [INC-20260412-002] 
# Swarm Trace 接口僵尸引用导致 500 崩溃 (AttributeError in Swarm Traces)

---

## 1. 事故描述 (Description)
- **触发点**: 访问 Dashboard 或 Swarm 监控页面时，前端请求 `/api/v1/agents/swarm/traces`。
- **症状**: 
    1. 后端报 `AttributeError: 'SharedMemoryManager' object has no attribute 'get_traces'`。
    2. API 返回 500 Internal Server Error。
    3. 前端由于这一核心监控接口失败，导致菜单加载异常或组件崩溃。

## 2. 根因分析 (Root Cause Analysis)

### 2.1 接口漂移 (Interface Drift)
- **发现**: 在之前的 M4/M5 架构升级中，虽然在 `observability.py` 中定义了 `SwarmTrace` 数据模型，但负责暴露数据的 `SharedMemoryManager` (Service 层) 并未同步实现相应的查询方法。
- **问题**: 路由层 (`agents.py`) 却已经提前引用并调用了这个不存在的方法。这属于典型的“快进式开发”导致的后端内部契约断裂。

## 3. 治理动作 (Remediation)
- **补全实现**: 在 `app/agents/memory.py` 中补全了 `get_traces` 的 Stub 实现，确保其能从 PostgreSQL 中检索并返回符合前端拓扑结构的 JSON 数据。
- **路由加固**: 增加了对 `memory_manager` 方法存在性的容错检查（虽然在核心规约下，我们更倾向于严谨的类型约束）。

## 4. 进化学习：新增规约 (New Governance Rules)

基于此事故，我们将补充 **[AGENT_CODING_RULES]**:
- **[RULE-B003] 零僵尸引用 (No Zombie References)**: 严禁在 API 路由层调用尚未在 Service 层正式实现的方法。如果需要先行占位，必须实现一个带 `TODO` 标记的 `NotImplementedError` 抛出逻辑，而非让系统因 `AttributeError` 崩溃。
- **[RULE-C004] 契约先行自检**: 智体在新增路由或修改现有路由调用时，必须通过 `grep` 验证被调用方法的物理存在性及参数签名匹配度。

---
*Generated by HiveMind Self-Learning Engine*
