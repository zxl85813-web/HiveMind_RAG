# 🚨 HiveMind 治理事故库 (Governance Incident Archive)

> **原则**: 每个由于规约不一致（Contract Drift）导致的问题，必须通过 `ContractGuard` 自动报告或手动登记。拒绝重复犯错。

## 📖 核心治理流程
1. **自动捕获**: 前端 `ContractGuard` 检测到字段异常（如 `Success` vs `success`）。
2. **强制记录**: 后端 `/api/v1/governance/incidents` 自动生成 Markdown 存根。
3. **入图与审计**: 事故将同步至 Neo4j 治理图谱，作为后续 Code Review 的“前车之鉴”。
4. **根因修复**: 开发者必须修改 `backend/app/common/protocol.py` 并同步更新前端类型。

## 📂 事故存根明细
| 编号 | 事故类型 | 影响组件 | 状态 | 关联任务 |
| :--- | :--- | :--- | :--- | :--- |
| [HIST-001] | Case Mismatch | SettingsPage | ✅ 已固化 | `protocol.py` |
| [HIST-002] | Auth Loop | api.ts | ✅ 已修复 | `RISK-001` |

---
*Generated by Antigravity Governance Engine.*
